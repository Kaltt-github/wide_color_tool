name: wide_color_tool_example
description: Interactive example application for the wide_color_tool package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  wide_color_tool:
    path: ..

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
