# wide_color_tool example

An interactive Flutter application demonstrating color-space conversion,
mixing, and WCAG contrast calculations with `wide_color_tool`.

## Run

```sh
flutter pub get
flutter run
```

The application is self-contained and references the package from the parent
directory through a local path dependency.
